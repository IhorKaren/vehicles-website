import { styled } from "@mui/material";

import { AppContainer } from "@/shared";

export const OrderPaymentContainer = styled(AppContainer)({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center",
  flexGrow: 1,
});
