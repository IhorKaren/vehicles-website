import { styled } from "@mui/material";

import { AppContainer } from "@/shared";

export const CreateOrderPageContainer = styled(AppContainer)({
  display: "flex",
  flexDirection: "column",
  flexGrow: "1",
});
